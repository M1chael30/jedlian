export default function MissionVisionWidget(title,description) {
    return(
        <div className="flex flex-col gap-2 justify-center items-center bg-amber-300">
            <h1>{title}</h1>
            {/* <p>{description}</p> */}
        </div>
    )
}